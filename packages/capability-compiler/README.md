# flychain-capability-compiler

Turns a natural-language capability description into a structured, auditable `CapabilitySpec` (YAML/JSON). The compiler runs the interview agent against the local Ollama judge by default, with optional OpenAI/Anthropic fallback.

Phase 0 ships the schema. Phase 3 ships the interview pipeline and templates.
